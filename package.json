{
  "name": "illegal-store-theme",
  "version": "1.0.4",
  "description": "Static HTML/JS implementation of Illegal Store Theme",
  "scripts": {
    "build": "mkdir -p public && cp index.html public/ && cp metadata.json public/ 2>/dev/null || :",
    "deploy": "npm run build && npx wrangler pages deploy public --project-name illegal-store"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}